URL: [https://amzn.asia/d/00GtVqf6](https://amzn.asia/d/00GtVqf6)

# Chapter1 チューニングの基礎知識

- 100ms遅いと0.2%、400ms遅いと0.6%の利用者減少
- 速いと処理に必要なリソースが少なくなる
- RTT(Round Trip Time)
	- リクエスト送信開始からレスポンス受信完了までの所要時間
- 同時並行処理性能をスループットと呼ぶ
	- 単位時間当たりのリクエスト処理数 rps(requests per second)
- 流動性の高いシステムリソース（負荷の影響を受ける）
	- CPU時間
	- メモリ領域
	- メモリI/O帯域
	- ネットワークI/O帯域
	- ストレージI/O帯域
- キャパシティの調整アプローチ
	- 垂直スケーリング（スケールアップ）
		- サーバーの性能を控除させてシステムリソースの総量を変える
	- 水平スケーリング（スケールアウト）
		- サーバーの数を増やしてシステムリソースの総量を変える
	- 技術以外の手法として先着順ではなく、抽選での販売にするなどの方法もある
- オートスケーリング
	- インフラにスケール変更を自動で行う機能
	- 需要変化からスケール変更完了までのリードタイムがある
	- インフラ基盤側のキャパシティが売り切れで増やせないことがある

## パフォーマンスチューニングきほんのき

- いきなり手を動かさない
- 勘で行動しない
- 基本のきから基本のんまで読んでから、考えて、行動する

- 推測せず計測する
	- 「推測するな計測せよ」
	- システムリソース利用状況、レイテンシ、スループットを計測する
- 公平に比較する
	- 前提条件をそろえて比較する
	- 意図的に変更したところ以外は同じ条件にして比較する
	- 対策実施後の計測は効果を期待してしまうため、誤解しがち
	- 小さなさにこだわらない読解力を持つ
- 一つずつ比較する
	- 複数案思いついても一つずつ実行する

## きほんのほ

構成要素や処理内容、処理方法を変えず調整することをイメージしがちだが、実務ではそうとも限らない
遅いから早いにするにはWebアプリケーションに手を入れる必要がある

- ボトルネックだけにアプローチする
	- ボトルネック外に手を入れても効果はない（たとえボトルネックが自分の得意外でも）
	- ボトルネック外に手を入れると逆効果のことも多い
- ボトルネックの特定は外側から順番に
	- マクロ視点で観測し、要素の入口出口で計測、システムリソースの利用状況を確認
	- CPU使用率が100%に張り付く、ネットワーク転送量が一定値になるなどが典型
	- CPU、メモリ、ディスクI/O、ネットワークI/Oなどがボトルネックになりがち
-  ボトルネック対処
	- 解決：課題を根本から解決
		- e.g.) 処理方法を変更する
	- 回避：迂回・省略する
		- e.g.) キャッシュを使いまわす
	- 緩和：影響を和らげる
		- e.g.) スケールアウト、アップなど

## きほんのん

負荷試験を実施 -> Webサービスを改善 -> 負荷試験を実施 -> …
1. 負荷試験計画
	1. なんのために、どのように、どの程度まで実施するかを決め、優先順位を明確化する
2. 実施準備
	1. 与負荷環境や被負荷環境を準備する
3. 負荷試行 -> 結果確認 -> 改善 -> 負荷試行 -> 結果確認 -> 改善 ->
	1. 負荷をかけながら利用して、使用感を確かめる
	2. 実施時間、実施結果、メトリクス、ログを記録
	3. 実施結果を都度解釈する
	4. 与負荷環境側のメトリクスも同時に確認

## まとめ

- Webサービスが高速であることは重要
- 高速かどうかを判断するために用いる指標はレイテンシ
- 速さとキャパシティは別指標だが関連している
- Webサービスの高速化は論理でアプローチできる

# Chapter2 モニタリング

モニタリングは継続的なテスト

アラート：正常状態でなくなった場合の通知
メトリクス：状態を定量的に示した値（レイテンシなど）
モニタリンググラフ：どの時間帯にどのような状態になっていたかを可視化しているもの
モニタリングダッシュボード；グラフなどの情報を一つの画面に並べられている

レイテンシ0.1秒だったものが徐々に1秒を超えたのか、突如1秒を超えたのかで事象が異なる

## モニタリングの種類

- 外形監視
	- 外側の観点からモニタリングする手法
	- ユーザとほぼ同じ経路を用いてアクセス
	- Synthtic Monitoring
	- シナリオテストを行うことも多い
- 内部監視
	- 内側からモニタリングする手法
	- メトリクスを取得し、リソースの状況を確認
	- webサービスの動作環境でモニタリング用のデーモンを立ち上げ、メトリクスの収集とモニタリングを行う <-- エージェント

## 手動でのモニタリング

### Linuxコマンド

`top` : CPU使用率
e.g.) $ top
`free`: メモリ使用率
e.g.) free -h

`tress`: CPUに負荷をかけることができる（メモリには影響なし）
e.g.) stress --cpu 2

## モニタリングツール

- メトリクスを自動収集・保存
- 保存したメトリクスをWebブラウザなどで見やすく表示
- メトリクスが閾値に達すると通知をする

## モニタリングツールアーキテクチャ

- プル型
	- モニタリングアプリがエージェントhwメトリクスを取得するアーキテクチャ
	- モニタリングアプリがクライアントとしてエージェントにリクエストを置くし、エージェントが収集、送信する
	- メトリクスの取得感覚をモニタリングアプリが管理できる
	- エージェントの実装をシンプルにできる
- プッシュ型
	- エージェントがモニタリングアプリへメトリクスを送信する
	- 1分などのタイミングでエージェントがメトリクスを収集し送信する
	- Webサーバーのポートの許可をモニタリングアプリに対してする必要がない
		- 内から外へのアクセスは緩く設定されていることが多い
	- 対象が増えてもモニタリングアプリケーションの設定変更が不要

今回例として挙げるPrometheusはプル型のアーキテクチャ
## node_exporter

- PrometheusがUnixライクな環境で動作させるエージェント
- Linuxホスト1台につき1つインストール
- Prometheusからリクエストを受け取るとメトリクスを収集しHTTPレスポンスとして返却
- cpu、メモリ、ディスクI/O、ネットワークなどの情報を取得可能

## モニタリングの注意点

### 高負荷状態のモニタリング

- モニタリングアプリケーションやエージェントもCPUやメモリなどのリソースを利用して動いている
- エージェントはWebアプリケーションと同じ環境で動作することが多いため注意が必要
	- エージェントは少ないリソースで動くようにはなっているが、注意は必要

- 負荷試験の場合は負荷試験によって発生する負荷についても考慮する
	- Webアプリケーション、エージェント、負荷試験を行うHTTPクライアントが必要になるが、同一サーバーにはおかない
	- エージェントは同一サーバーに置く
		- 高負荷状態（本番環境）でもサーバーに存在しているものだから

### モニタリングの解像度

1分おきにメトリクスを収集している場合、30秒間の異常を見逃してしまう
-> 解像度が低い、足りていない


プロファイラ：アプリケーション内部のリソース利用について調査するツール
ラインプロファイラ：ソースコードのどの行でどのくらい時間がかかっていたか可視化するツール

## ログに対するモニタリング

- 基本、1アクセスに対して1行のログが出る

## まとめ
- モニタリングとは
- モニタリングの種類
- モニタリング手法
- モニタリングの考慮すべき点

# Chapter3 基礎的な負荷試験

ベンチマーカー: 性能を数値化するための負荷試験を実行するソフトウェア
Webサービス側で性能計測できることは重要<-早く問題に気づくことで安定的な運用が可能になる

## アクセスログを集計する

- Webサーバーのnginxのログをとる
- リクエスト、レスポンスや処理完了時間などが含まれるが、フォーマットがずれる可能性があるのでjson形式で出力させる

alp: goで実装されたアクセスロつ解析ツール。短期間の解析にはよいが、長期間、複数サーバーのログを扱うことは苦手

### Nginx

役割
- Webサーバー機能（静的ファイルの配信）
- リバースプロキシ（APIサーバー等への転送）
- キャッシュ機能
- ロードバランサー（負荷分散）

- **マスタープロセス（Master Process）：** 全体の司令塔。設定ファイルの読み込みや、後述のワーカープロセスの管理を行います。
- **ワーカープロセス（Worker Process）：** 実際にクライアント（ブラウザ）からのHTTPリクエストを受けて処理する実体です。CPUのコア数に合わせて複数立ち上がります。

## ベンチマーカー

ab: Apache HTTP Serverに付属するコマンド。特定のURLへリクエストを送信
クライアントとサーバー間の通信速度がボトルネックになる可能性があるため、十分な帯域、低レイテンシが確保された場所（同一リージョンなど）から行うとよい

## アクセスログのローテーション

ローテート: アクセスログを負荷試験ごとに別名に変更すること

## パフォーマンスチューニング 最初の一歩

Requests per secondを使うのを推奨。
- 改善すると向上する値である
- レイテンシだと利用者の体験に変化がなくなる下限が存在する

## MySQLのボトルネック

- スロークエリログにメトリクスなどが保存されている
- デフォルトでは出力されない
- 一般的には設定で遅いクエリのみを記録する
- パフォーマンスチューニングではすべてのログを記録することを推奨
	- 計測結果に影響があるのでそこだけ注意

## スロークエリログの解析

- mysqldumpslowコマンドで解析解析可能
- 3行の結果を返すために10万行読み取り50msかかっていた
- インデックス作成後は9行の読み取りを0.3msまで短縮

## 負荷試験による確認

- ボトルネックが移動していることを確認
- **一つの変更ごとに一つの負荷試験を行う**

## ベンチマーカーの並列度

Requests per secondは変わらず、Time per requestがどんどん大きくなる
- サーバーの処理能力が飽和しているため、同時にリクエストを処理すると並列度の分だけ悪化する

-> 本当にサーバー処理能力をすべて使い切っているのか確認する

## まとめ
- Webアプリケーションで性能を計測するログ出力と集計方法
- ベンチマーカーによって負荷を与える方法
- 負荷試験中のサーバーリソースモニタリング
- ログの解析によるボトルネックの発見
- データベースへのインデックス付与による性能改善
# Chapter4 シナリオを持った負荷試験

実際のサービスではどのURLがボトルネックになっているか知るために負荷試験を使うほうが多い -> abコマンド < 負荷試験ツール

## 負荷試験ツールk6

- 負荷試験のためのOSS
- 開発者の使いやすいAPIをもったCLIを提供
- シナリオはJavaScriptで記述可能
- 実行エンジンはGoで実装

負荷試験のシナリオとは
1. URLにリクエストを送信する
2. サーバーから帰ってきたレスポンスの結果を解析する
3. HTML内にあるリンクをたどる/フォームをsubmitして次のリクエストを送る
4. 繰り返し

## 負荷試験の適切な実行時間

負荷試験の時間は5分くらいが推奨
->リソースのモニタリングツールの周期が15~60秒
->輝度直後などだと通常作られるはずのキャッシュや最適化が走っていない可能性がある

## まとめ

- 負荷試験を行うためのシナリオを持ったベンチマーカーの作成方法の紹介
- 得られた結果やログの読み取り方の解説

実際のユーザーのアクセスパターンを参考にシナリオを作成するほうが妥当性が高い

# Chapter5 データベースのチューニング

データベースは保存データを失わないこと、データ不整合を起こさないこと、すぐ不整合を発見できること、常に高速なレスポンスを返すこと、を求められる

### RDBMS（リレーショナルデータベース）

- データを表のような形式で扱うシステム
- SQLによる問い合わせや強い一貫性が特徴
- MariaDB, PostgreSQL, SQLiteなど
- 複数サーバーに分散させてスケーラビリティ向上を目指すのは難しい
- データの整合性のために処理時間が長い

## NoSQL

- 強い一貫性がない代わりに高速な処理を実現する
- 複数サーバーに分散して高いスケーラビリティを持つ
- 例）memcached、Redisなど
	- キャッシュ用のKey Value Store(KVS)
	- データを全てメモリ上で制御するため、高速で動く
- 一般的な文字列、Lists, Sets, Sorted sets, Hash


## NewSQL

- 一貫性と分散を両立
- RDBMSからNoSQLへの移行は、トランザクション管理やデータ不整合対策に求められる負担が大きかった -> NewSQL
- Cloud Spanner(GCP), TiDB, Cockroach DBなど

## データベースの選択

- 規模が大きなサービスでない限りRDBMSでよさそう

## インデックスの役割

- インデックスなし
	- 普通の本を読んで単語の数を数える
- インデックスあり
	- 辞書のように単語が整列している
	- 二分探索
	- e.g.) データベースを調べる場合
		- 真ん中を開く -> な
		- 最初と「な」のページの真ん中を開く -> し
		- 繰り返す

- オプティマイザ: SQLクエリを解析し、インデックスの使用を決定する役割を担う

- ソート処理はデータベースにとって負担が大きい処理のひとつ
- ソート処理もインデックスで行えるとよい（post_id + created_atの複合インデックスなど）

### プライマリインデックス

- カラムに格納される値がすべてユニークな値になる、テーブルを代表するデータ（プライマリキー）に貼られるインデックス
- シーケンシャルな数字やUUID

### セカンダリインデックス

- プライマリインデックス以外のインデックス
- Covering index: カウントなどセカンダリインデックスに含まれる情報だけで返せる最適化

### クラスターインデックス

- セカンダリインデックスの先にはプライマリインデックスが存在しており、実際のデータにはプライマリインデックスでもう一度探す

## アンチパターン

- すべてのカラムに対してインデックスを張るようなパターン
- インデックスはソート済みのもう一つのデータベースなので追加更新があった時にはオーバーヘッドが増える
- あまり頻繁に使われないものは最新のデータから確認するとよい（新しい方が頻繁にアクセスされるはず）
	- 日付のインデックスを用意しておけば、ソート処理も必要ない

## N+1

- 1回のクエリで得た件数（N件）に対して関連情報収集のためにN回以上のクエリを実行してしまうこと
	- 投稿一覧を取得（N件）し、一つ一つの内容のユーザー情報を取得するなど
- ログを取得して、実行数が多いクエリをヒントにWebアプリケーションのループ中にSQLを実行している場所がないか探すなど

解決策
- キャッシュ
	- キャッシュサーバーが必要になる
	- キャッシュに対するN+1問題が発生する可能性がある
- 別クエリでプリロード
	- N回問い合わせるのではなく、データをリスト化してクエリを投げる
- SQLクエリのJOIN
	- 投稿一覧+ユーザー情報をいっぺんにデータベースに聞く

## まとめ

- RDBMS/NoSQL/NewSQL　データベースの種類と特徴
- データベースのプロファイリング
- インデックスの理解と活用
- N+1問題の発見と解消

# Chapter6 リバースプロキシ

リバースプロキシ: リクエストを受け取り上位サーバーのアップストリームサーバーに送る
- 負荷分散（ロードバランス）
- コンテンツのキャッシュ
- HTTPS通信の終端

## プロセスとスレッド

- マルチプロセス・シングルスレッド
	- コンテキストスイッチが問題
	- キャッシュ切り替えでパフォーマンス低下
	- C10K問題（クライアント数が1万こえたあたりで大幅低下）

## nginx

- 静的ファイルをキャッシュしておける
- イベント駆動アーキテクチャ
	- 非同期APIを使うイメージ
- gzipを使ってbodyのデータサイズ圧縮（およそ1/5になる）
	- 毎回圧縮する方法と最初に圧縮しておく方法、アプリケーションサーバーで圧縮しておく方法などがある
- リクエスト/レスポンスのバッファリング
	- 遅いクライアントがいてもアプリケーションサーバーには影響を及ぼさない
- アップストリームサーバーとのコネクションをずっとつないでおく方法もある
## まとめ

- リバースプロキシを前段に置く理由
- プロセス・スレッドについて
- nginxのアーキテクチャ
- nginxを活用した高速化手法

# Chapter7 キャッシュの活用

- Webアプリケーション上で作ったキャッシュをミドルウェアなどに保存して利用する方法
	- この章ではこちらがメイン
- HTTPレスポンス全体をキャッシュする方法

## ミドルウェア

- KVSとしての機能
	- シリアライズ/デシリアライズ機能が必要
- TTL（Time To Live）が過ぎたらexpireして削除する機能

e.g.) memcached, Radis

### インメモリに保存

- 適切なロックをとる必要がある
- マルチプロセスの場合はメモリを容易に共有できない
- デプロイ直後にキャッシュが存在しない
- 問題あるデータをキャッシュしたときに容易に消せない
	- 十分短いTTL設定が必要

## キャッシュ利用

<メリット>
- パフォーマンスが上がる
	- インフラコストが下がる
	- 負荷の大きい、時間のかかる処理の実行回数が抑えられる
- 大量リクエストに耐えられる

<デメリット>
- 古いデータが取得されうる
- キャッシュを管理するミドルウェアが新たな障害点になる
- 想定外のデータを表示するバグにつながる（情報流出）
- 問題発生時の原因究明難易度が上がる
- キャッシュに乗ってないタイミングでの大量リクエストによって、キャッシュ生成の重い処理が同時に実行されうる
	- Thundering herd problem

<観点>

- データの不整合がどこまで許容されるか
- データの特性的にキャッシュを使う必要があるか
	- ユーザー固有のデータの場合、他のユーザーに使いまわせない
- データの更新頻度
- データの生成コスト

### TTL

- 十分短い時間を設定する
- データ更新時にキャッシュも同時更新する

## 実装方法

- キャッシュにデータなし -> キャッシュ生成して結果を保存
- キャッシュにデータなし -> デフォルト値や古いキャッシュを返す -> 非同期にキャッシュ更新処理
	- Thundering herd problem未解決
- バッチ処理での定期キャッシュ更新
	- Thundering herd problem解決
	- ほぼ使われないキャッシュの管理が必要
	- 障害などでキャッシュが揮発したときにふっきゅうに　時間がかかる

## キャッシュの監視

- exprireしてないのにキャッシュから追い出されたアイテム数
	- キャッシュ容量不足
- キャッシュヒット率

## まとめ

- キャッシュ保存に利用するミドルウェア
- キャッシュを使うタイミング
- キャッシュの手法
- Thundering herd problemなどキャッシュを使うときに発生する問題や注意点
- キャッシュの監視

# Chapter8 押さえておきたい高速化手法

## 外部コマンド実行（シェル実行）ではなくライブラリを利用する

- シェルを実行する
	- Cでいうfork+exec
- OSコマンドインジェクション脆弱性の問題がある

対策
- 外部コマンド呼び出しを利用しない
- 外部から入力されたパラメータを使わない
- 安全な関数を使用してエスケープする

## 冗長なログ出力をしない

- 開発用設定を切る
	- デバッグモードが有効
	- ログレベルがDEBUG
	- クエリをすべて保存している

## HTTPクライアントの使い方

- 同一ホストへのコネクションを使いまわす
	- TCPやTLS初期通信が必要になる
	- HTTPプロキシサーバーを用意してアプリケーションサーバーとそこは毎回張りなおすが、プロキシサーバーと通信対象の間のコネクションは張り続ける
- 適切なタイムアウト設定
	- 処理中リクエストが大量に溜まる
- 対象ホストへのコネクション数の制限を確認する
	- デフォルトは2個90秒とか

## 静的ファイルをリバースプロキシから配信する

- データベースやネットワークリソースを節約できる
- アプリケーションサーバーが画像データを用意しなくてよくなる

## HTTPヘッダーを利用してクライアント側にキャッシュさせる

- 初回 or キャッシュなし -> 通常のリクエスト
	- サーバーからLast-Modified or ETagが返ってくる
- キャッシュ期限切れ後のリクエストはヘッダにIf-Modified-Since or If-None-Matchヘッダを付与しリクエスト
	- コンテンツに変化なければ、ボディが空の304 NOT MODIFIEDを返す -> キャッシュが使われる
	- 変化あれば、新規のデータ+ Last-Modified or ETagが返ってくる

## CDN上にHTTPレスポンスをキャッシュ


CDN（Content Delivery Network）とは、Webサイトの画像や動画などのコンテンツをユーザーにすばやく届けるためのネットワークです。

- **キャッシュサーバーの活用**: 世界各地に配置された中継用のサーバー（エッジサーバー）に、元のWebサーバー（オリジンサーバー）のデータを保存（キャッシュ）します。
- **近い場所から配信**: アクセスしたユーザーに一番近い場所にあるサーバーからデータを返すことで、表示スピードを速くします。

- 柔軟なキャッシュ設定
- アクセスログなどのログ保存
- HTTPリクエスト/レスポンスの書き換え
- DDoS対策
- Web Application Firewall
	- 悪意あるリクエストを遮断したり、ログを取得する

## まとめ

- アプリケーションから外部コマンドを呼び出すデメリット
- デバッグモードで冗長なログを出力委ｓ内
- HTTPクライアントの適切な扱い方
- 静的ファイルを配信する際の注意点
- Cache-Controlヘッダーの活用方法
- CDNの利用

# Chapter9 OSの基礎知識とチューニング
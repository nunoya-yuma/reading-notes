[https://amzn.asia/d/01QL5CSJ](https://amzn.asia/d/01QL5CSJ)

GoFのデザインパターン
URL: [https://amzn.asia/d/01QL5CSJ](https://amzn.asia/d/01QL5CSJ)

# GoFのデザインパターン

具体的なクラス名などが分からなくても操作できると、より疎結合になります。
（コピーや共通のAPIなどを定義するクラスがあり、クライアントはそのクラスのみを知っており、そのメソッドなどを呼び出すイメージです。）

*   誰が何を知っているか、どのクラスがどのメソッドを知っているかを意識する
*   知らないからこそ交換が可能になる

-   抽象クラスやインターフェースを使って、具象クラスから抽象メソッドを引き離す
-   引数でインスタンスを渡すときや、フィールドでインスタンスを保持するときは、具象クラスではなく抽象クラスやインターフェースの型にしておく

継承よりも移譲の方が緩い結びつきになります。
既存のクラスに修正を加えずに拡張ができる設計を目指します。

インターフェースは少なくするべきです。
-> クライアントはどれを使えばいいか分からなくなり、順番に気を付けないといけなくなるため、外部との結合を疎にするためです。

-   分割して統治せよ
-   大きい複雑なものは小さくて易しい問題まで分割して解決する

# 第一章　Iterator

要素を数え上げる機能を提供します。
例: `hasNext()`, `next()`

Iteratorというインターフェースさえ知っていれば、何を数え上げていて、どんな実装になっているのか（配列かそれ以外かなど）を気にしなくてよくなります。

# 第二章　Adaptor

交流 -> adaptor -> 直流 のイメージです。
既存のクラスに一枚挟まるイメージで、すでにある実装を使って、定められたインターフェースの振る舞いをするクラスを作成します。

既存のコードに手を加える必要がなく、再度テストする必要がないという利点があります（ただし、注意が必要な場合もあります）。

```java
class Foo {
    OtherTool tool; // もしくは継承してメソッドを使えるようにする

    void fooApi() {
        tool.api();
    }
}
```

# 第三章　Template method

親クラスがひな形（テンプレート）を用意し、子クラスが細かい動作を定義します。  
(Trait -> 各Traitのようなイメージです)

継承先クラスは継承元のクラスの型としても振る舞える必要があります。  
（共通のメソッドから継承先のメソッドがうまく動かない、といった事態は避けるべきです。）

# 第四章　Factory method

- コンストラクタではなくメソッドを呼び出してオブジェクトを作成します。
- Factory, Productの二つのクラスが登場します。
    - FactoryはProductのインスタンスを作成する目的を持ちます（Productの実装を知っている）。
    - Productは実際に使われる機能を提供します。

FactoryやProductが具体的な何かに継承されて、オーバーライドされるイメージです。

# Singleton

PCそのものやシステムの設定、ウィンドウマネージャーなど、インスタンスを一つしか許さない場合に適用されます。  
スレッドセーフな実装にする必要があります（インスタンスは最初に生成しておくなど）。

# Prototype

クラスからのインスタンスの生成ではなく、他のインスタンスをコピーして作成します。  
コピーを対象のクラス名を知っていなくても操作できます。  
メンバ変数の状態も含めてコピーします。

# Builder

複雑なオブジェクトの構築処理を継承先のクラスで実装し、クライアントはそのインスタンスをBuilderを扱うクラスに渡して動作させます。

Main -> Director -> Builder (PrintBuilder or HtmlBuilder)  
MainがPrintBuilderまたはHtmlBuilderを作成してDirectorに渡します。  
DirectorはBuilderのメソッドだけを使って様々な処理を行います。

# Abstract Factory

抽象的な部品と、それを使って抽象的な作成をつかさどるFactoryを定義します。  
それらを具体的な処理を担当するサブクラスが存在します。

# Bridge

機能の追加と実装方法を分離できます。

- **機能のクラス階層**: 新しい機能をクラスの継承によって追加します。  
    例: `ClassA` -> `ClassB` (`ClassA` + `API_new1`)
- **実装のクラス階層**: 抽象化されていたクラスに実装するレイヤとしてクラスを継承します。  
    例: `AbstractClassA` -> `ImplementClassB`

機能の実現については機能のクラス層で定義しておき、実装のクラス層でAPIを定義し、それを継承した先でOS固有の処理を行わせる、といった使い方ができます。

# Strategy

アルゴリズムの実装部分を変えられるパターンです。  
クラスのコンストラクタにアルゴリズムを実装したクラスを渡すようなケースで利用されます。

# Composite

フォルダの中にファイル、もしくはフォルダを（再帰的に）入れることができるパターンです。  
これはファイル（中身）とフォルダ（容器）を同一のディレクトリエントリとして扱うことで実現され、このパターンがそれを可能にします。

- 複数と単数の同一視
- 中身を気にせず`add`できる

`getSize()`を呼び出すときは、すべてのエントリの`getSize()`メソッドを呼び出せばよく、ディレクトリの場合は再帰的に呼び出されていきます。

# Decorator

ケーキのスポンジ -> 生クリーム -> いちご のように機能を追加していくイメージです。  
文字列を表示するクラス（`display`）に、挿入された`display`に飾りをつける機能を持ったクラス（`display`と同じクラスを継承）を追加します。

文字列の表示と飾りつけを同じクラスとして扱うことで、継承を重ねることでたくさんの機能を持ったクラスを実現できます。

# Visitor

データ構造と（そのデータに行う）処理を分離するパターンです。  
Fileなのかディレクトリなのかによって処理を変更します。  
ディレクトリだった場合はVisitorを受け入れてもらい、Visitorの処理をする、というのを再帰的に繰り返します。

# Chain of responsibility

- ある要求を処理するオブジェクトをすぐに決められない場合に、たらいまわしのように探すパターンです。
- そのオブジェクトで処理できるなら処理し、だめなら（Chainのように紐づいている）次のオブジェクトに回します。
- 要求を出す人が処理の頭を持っていなくてもよく、ただ投げればよいので、独立性や部品化が促進されます。
- 動的に処理の順番などを変えることができます。
- 処理が重くなってしまう可能性があるので注意が必要です。

# Façade（建物の正面）

各クラスがそれぞれを知って、適切にメソッドを呼び出す必要がある場合に、`Facade`という窓口に投げて細かいことをやってもらいます。  
例: `makeWebPage(username, filename)`のようなメソッドで、データベース管理クラスやHTMLファイル作成クラスのメソッドを適切に呼び出す、といった使い方です。

インターフェースは少なくするべきです。  
-> クライアントはどれを使えばいいか分からなくなり、順番に気を付けないといけなくなるため、外部との結合を疎にするためです。

FacadeをまとめたFacadeを作るのも有効です。

# Mediator

メンバーは相談役にだけ報告し、相談役だけから指示が来るパターンです。  
ログイン画面で、ユーザーかゲストか、ユーザー名のフィールド、パスワードのフィールド、OKボタン、キャンセルボタンがあったときに、各状態の変化があった時にMediatorに知らせ、Mediatorがルールに沿って各Colleagueに有効・無効の指示を送ります。

Mediatorが複雑になる可能性はありますが、バグが発生したのならここだとすぐに特定できます。  
Mediatorクラスは再利用性が低い可能性が高いです。

# Observer

観察対象の状態変更に応じて動作するパターンです（Pub-Subの関係に近いです）。  
観察される側のクラスにObserverのクラスのインスタンスを登録しておき、状態が変化したタイミングで呼び出してもらいます。

- 複数登録する場合はIteratorのデザインを併用するとよいです（呼び出される順序に影響されないようにしておく必要があります）。
- 登録や削除を許容すれば、交換可能になります。
- 通知のときに渡す情報の範囲は抽象化しつつも、範囲が限られることが望ましいです。
- 通知 -> 変化 -> その変化の通知 -> 変化、という無限ループにならないようにする必要があります。

# Memento（記念品、思い出の種）

インスタンスの状態を表す役割を導入して、カプセル化を破壊せずに保存と復元を行うパターンです。  
Undo, Redo, History, Snapshotなどを実現します。

ゲームなど、保存される側にMementoを使う実装を入れないようにします。  
-> MainがGameやMementoを使う形で、必要に応じてGameの状態を保存したり、復元したりするようにします。

Mementoとして保存しておいても、ずっと保存していた場合にバージョンアップに耐えられない可能性もあるため、賞味期限を考えておく必要があります。

# State

状態をクラスで表すパターンです。  
昼間のクラスと夜間のクラスを用意して、そのどちらかをContextがStateとして保持します。  
イベントが来たときに、保存しているStateのメソッドを呼び出します。

各Stateのメソッド内でContextが持つべきStateを決める（変更する）こともできます。  
-> Stateが頭を持てばStateが他のStateを知らねばならず、Contextが持てばContextがすべてのStateを知らねばならない、という点で一長一短があります。

# FlyWeight

インスタンスを共有して無駄に`new`しないパターンです。  
Factoryクラスを通して0〜9までをASCIIアートで表したデータを作成します。  
このとき、すでに作成されているものはMapの形式で持っておき、以前作られたものはそれを流用します。

それぞれが`new`するのではなく、Factoryで作ることで、すべてのオブジェクトを管理することができます。  
変更したときに良いか悪いかは別にして、すべてに影響します。  
メモリ以外にも生成の時間を短縮できます。

# Proxy（代理人）

まずは代理人を立てておいて、必要になった時にインスタンスを作成したり、情報を本人に聞きに行ったりするパターンです。  
本体と外部に対しては同じ`public`メソッドを持っておき、インスタンスが生成されていれば本体のインスタンスを持っておいて、そちらのメソッドも呼び出して処理をします。

- 本人のインスタンスが生成されているかどうかはProxyを利用する側は意識しません（させません）。
- 本人もProxyという存在から呼ばれていることは知りません。
- 例えば、インスタンスの作成や初期化に時間がかかる場合、それが本当に必要になったタイミングで実行されることが望ましいです（ホームページの画像を、あとで開くときではなく、実際にスクロールされて表示されるときに画面に読み込むようなイメージです）。
- Proxyなのか本人なのかを使う側は意識しなくてよいので、あとでProxyを使わずに直接本人を使ってもよいです（継承元のインターフェースでオブジェクトを扱っておく必要があります）。

# Command

命令をするときにそれを表現するインスタンスを作成するパターンです。  
インスタンスの順序が命令の順序になります。  
命令（Command） = イベント と考えることもできます。

ある位置に点を描画するCommandをMacroCommandsが持っておき、変化があるたびに再描画します。  
MacroCommandsでは、`add`や`execute`するCommandがCommandなのかMacroCommandsなのか意識しなくてよいです。

# Interpreter

プログラムが解決しようとする課題を解決する「ミニ言語で書かれたミニプログラム」を、Interpreterが実装している言語で解釈して、実行環境が実行するパターンです。  
-> 修正が必要な場合に、実装している言語ではなくミニプログラムを変えればよいという利点があります。

ミニ言語の例: `go`, `left`, `right`, `repeat`などの簡単な命令を組み合わせて車を動かす言語のようなものです。  
例:

- `go, right, right, go`
- `go, right, go, right, go, right, go, right`
- `repeat 4 go, right`

BNF (Backus Normal Form) という文法があります。  
こういったミニ言語の構文解析を行います。